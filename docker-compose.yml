version: "3"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
  
  redis:
    image: redis

  prometheus:
    image: prom/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    ports:
      - "3000:3000"
      
